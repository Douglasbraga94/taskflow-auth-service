server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: auth-service

jwt:
  secret: ${JWT_SECRET:0OL3hz0pZb6pqe6yKvcHFwCqKwIaQZmT9RlSMfgcmRx}
  expiration: ${JWT_EXPIRATION:3600000}

services:
  user-service:
    url: ${USER_SERVICE_URL:http://localhost:8081}
